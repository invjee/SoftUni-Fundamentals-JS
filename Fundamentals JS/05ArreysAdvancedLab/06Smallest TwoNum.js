function sort(arr){
    //arr.sort((a,b)=>(a-b))
    //let result = 
    console.log(arr
        .sort((a,b)=>(a-b))
        .slice(0,2)
        .join(' ')
        )

}
sort([30, 15, 50, 5])