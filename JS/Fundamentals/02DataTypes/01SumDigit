function sum(num){
    let str = num.toString()
    let sum = 0
    for(let i = 0; i< str.length; i++){
        let digit = Number(str[i])
        sum += digit
    }
    console.log(sum)

}
sum(245678)
